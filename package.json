{
  "name": "fuatilia",
  "version": "0.1.0",
  "private": true,
  "description": "Fuatilia (Swahili: to track, to follow up) — AR & Collections platform for Kenya. Ledger-first domain core.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/pg": "^8.23.1",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  },
  "dependencies": {
    "pg": "8.23.0"
  }
}
